public class SimulationOutils {

    public static final int MAX_LIGNE = 8;
    public static final int MAX_COLONNE = 8;

    public static int randomInt(int num) {
        return (int)(Math.random() * (num)) + 1;
    }
}